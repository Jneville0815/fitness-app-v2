import { createTheme } from '@mui/material/styles'

const theme = createTheme()

export const calculateMacrosStyles = {
    heightField: {
        width: '11.5ch',
        margin: theme.spacing(1),
    },
    submittedBox: { marginTop: 2 },
}
